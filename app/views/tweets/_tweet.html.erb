  <% if tweet == @tweet %>
    <tr id="highlight_me">
  <% else %>
    <tr>
  <% end %>
    <td><%= tweet.message %></td>
    <td><%= getLocation(tweet.location) %></td>
    <td><%= link_to 'Show', tweet %></td>
    <td><%= link_to 'Edit', edit_tweet_path(tweet) %></td>
    <td><%= link_to 'Destroy', tweet, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>

